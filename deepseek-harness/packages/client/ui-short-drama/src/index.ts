/** Host loader entry for the browser-only Zenwit product shell. */

/** Provides no host-side behavior yet (project library host service arrives with the Remote wiring). */
export function apply(): void {}
